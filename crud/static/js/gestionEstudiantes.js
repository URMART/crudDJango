(function () {

    const eliminacion = document.querySelectorAll('.eliminar');

    eliminacion.forEach(btn => {
        btn.addEventListener('click', (e) => { const confirmacion = alert('Estudiante eliminado') })
    }
    )

})();


